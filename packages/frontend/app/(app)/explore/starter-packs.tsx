import React from 'react';
import { StarterPacksTab } from '@/components/StarterPacksTab';

/**
 * Explore › Starter Packs (route `/explore/starter-packs`) — curated packs of
 * accounts the viewer can follow one-by-one or all at once.
 */
export default function ExploreStarterPacksScreen() {
  return <StarterPacksTab />;
}
