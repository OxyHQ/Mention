# Local override for on-device testing: point at PRODUCTION Mention API + socket
# so posts load (prod backend has valid Oxy service creds). Oxy auth already prod.
# Remove this file to go back to the local backend (.env → localhost:3000).
EXPO_PUBLIC_API_URL=https://api.mention.earth
API_URL_SOCKET=wss://api.mention.earth
